import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/', // For traditional web hosting (change to '/subfolder/' if deploying to a subfolder)
})

